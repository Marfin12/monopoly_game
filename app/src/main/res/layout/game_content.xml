<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include
        android:id="@+id/top_city"
        layout="@layout/city_top" />

    <include
        android:id="@+id/left_city"
        layout="@layout/city_left" />

    <include
        android:id="@+id/right_city"
        layout="@layout/city_right" />

    <include
        android:id="@+id/bottom_city"
        layout="@layout/city_bottom" />


    <include
        android:id="@+id/animated_card"
        layout="@layout/item_card"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
